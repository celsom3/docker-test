// import Boom from 'boom';

const sessionController = {
  login(req, reply) {
    reply('Loggin in...');
  },
  logout(req, reply) {
    reply('Loggin out...');
  },
};


export default sessionController;
